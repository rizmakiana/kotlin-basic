fun main(){
    val numbers = arrayOf(1,2,3,4,5)

    for (value in numbers){
        println(value)
    }

    // there is no traditional for loop like java -> for(initialization; condition; increment)
}